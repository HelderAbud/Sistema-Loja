# IDs de CVE a ignorar temporariamente (um por linha), só com justificativa no PR.
# Preferir atualizar a imagem base (JRE Alpine) ou dependências em vez de alargar esta lista.
#
# Exemplo (não descomentar sem revisão de segurança):
# CVE-2024-12345

# GHSA-qwww-vcr4-c8h2 (react-router >=7.12 <8.3) — só afeta unstable RSC APIs.
# LojApp SPA usa BrowserRouter (sem RSC). Fix upstream é major 8.3.0; acompanhar em fatia dedicada.
GHSA-qwww-vcr4-c8h2
