# Copiar para um ficheiro ignorado (ex.: local/agent-runs/.env na raiz do repo — já coberto por .gitignore .env*)
# ou exportar na sessão do PowerShell antes de correr o script.

LOJAPP_BASE_URL=http://localhost:8081
# Obter com login (não versionar o valor):
# LOJAPP_ACCESS_TOKEN=

# Opcional — só para GET de stock no script de leitura:
# LOJAPP_PRODUCT_ID=1
