# Backend CI (legado) — DESATIVADO
#
# A pipeline canónica é `.github/workflows/ci.yml` (unit + integration + frontend + Trivy).
# Este ficheiro permanece só para histórico; não dispara jobs.

name: Backend CI (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use ci.yml"
